///cgrid_set_collision(val)
var val = argument0;
ds_grid_add_region(obj_cgrid.cgrid, x / obj_cgrid.cgrid_size, y / obj_cgrid.cgrid_size, (x / obj_cgrid.cgrid_size) + 1, (y / obj_cgrid.cgrid_size) + 1, val);

