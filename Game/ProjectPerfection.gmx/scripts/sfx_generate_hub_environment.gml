///sfx_generate_hub_environment()

/*
with(obj_player)
{
    if(speed > 0)
    {
        sfx_play_volume(snd_footsteps_concrete, false, 1);
    }
    else 
    {
        sfx_stop(snd_footsteps_concrete);
    }
}
*/
