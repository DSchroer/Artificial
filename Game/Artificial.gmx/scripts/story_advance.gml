///story_advance(delay);
var delay = argument0;
exit;
/*
if(delay <= 1)
{
    var dialog = story_get_dialog();
    if(dialog != -1)
    {
        story_update();
    }
    ui_textbox_queue(dialog);
    ui_flash_textbox();
}
else
{
    with(obj_player)
    {
        alarm[8] = delay;
    }
}
*/
